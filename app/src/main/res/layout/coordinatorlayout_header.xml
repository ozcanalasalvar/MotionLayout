<?xml version="1.0" encoding="utf-8"?>
<com.ozcanalasalvar.MotionLayoutExample.view.CollapsibleToolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/motionLayout"
    app:layoutDescription="@xml/scene_header"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="50dp"
    android:background="#1e376b"
    app:layout_scrollFlags="scroll|enterAlways|snap|exitUntilCollapsed">

    <ImageView
        android:id="@+id/background"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@color/colorAccent"
        android:scaleType="fitCenter"
        android:src="@drawable/budapest"/>

    <ImageView
        android:id="@+id/menuImage"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/menu_icon"
        android:tint="@color/colorWhite" />
    
    <TextView
        android:id="@+id/label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:transformPivotX="0dp"
        android:transformPivotY="0dp"
        android:text="Budapest"
        android:textColor="#FFF"
        android:textSize="32dp" />

    <TextView
        android:id="@+id/sublabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Budapest"
        android:textColor="#FFF"
        android:textSize="24sp"
        app:fontFamily="cursive" />

</com.ozcanalasalvar.MotionLayoutExample.view.CollapsibleToolbar>